import { println } from "std/fmt"

fn main() {
    let My_Var_Umlaut = 10 // Violates lowerCamelCase (due to underscore and initial capital)
    let another_Bad_Name = 20 // Violates lowerCamelCase
    println(My_Var_Umlaut + another_Bad_Name) // We need to use them to avoid unused-variable issues
}
