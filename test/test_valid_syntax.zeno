// Test file with valid syntax but missing types

// Function with missing return type (should fail validation)
fn add(a: int, b: int) {
    return a + b
}

// Function with missing parameter types (should fail validation)
fn subtract(x, y) {
    return x - y
}

// Function with proper types (should pass validation)
fn multiply(a: int, b: int): int {
    return a * b
}

// Main function doesn't need explicit return type
main() {
    let result = multiply(3, 4)
    println(result)
}
